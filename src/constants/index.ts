export const API_BASE_URL = "/api"; // In a real app, this would come from an environment variable

export const STORAGE_KEYS = {
  SCHOOL_DATA: "school-storage",
  THEME: "theme-storage",
};

export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 10,
};
